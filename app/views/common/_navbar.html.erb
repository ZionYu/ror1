<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <p><%= link_to 'RoR1', root_path, class:"navbar-brand" %></p>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav ml-auto">
      <% if !current_user %>
        <%= link_to("註册", new_user_registration_path,class:"nav-link") %>
        <%= link_to("登入", new_user_session_path,class:"nav-link") %>
      <% else %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Hi!,<%= current_user.email %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <%= link_to("My Groups", account_groups_path, class:"nav-link") %>
            <%= link_to("My Posts", account_posts_path, class:"nav-link") %>   
            <%= link_to("登出", destroy_user_session_path, method: :delete, data: {confirm:"Are you sure?"}, class:"nav-link") %>
          </div>
        </li>
          
          
          
          
      <% end %>
    </div>
  </div>
</nav>